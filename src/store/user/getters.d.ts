import type { UserState } from './typings';
declare const _default: {
    username: (state: UserState) => string;
    password: (state: UserState) => string;
    index: (state: UserState) => number;
};
export default _default;
